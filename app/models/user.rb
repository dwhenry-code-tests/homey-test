# User does not currently have any fields as is just a placeholder
# in a real system I would likley use Devise or user management, with
# the plan to bring this to an inhouse system when (and if) required.
class User < ApplicationRecord
end
